const tileLeve1 = [
  [tile, tile, tile, tile, tile],
  [tile, tile, tile, tile, tile],
  [tile, tile, tile, tile, tile],
  [tile, tile, tile, tile, tile],
];

const tileLevel2 = [
  [tile, tile, tile, tile],
  [tile, tile, tile, tile],
  [tile, tile, tile, tile],
];

const tileLevel3 = [
  [tile, tile, tile],
  [tile, tile, tile],
];

const boardLayout = [tileLevel1, tileLevel2, tileLevel3];
