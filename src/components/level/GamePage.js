import React from "react";
import BottomBar from "./BottomBar";

const GamePage = () => {
  return (
    <div>
      <button>Restart</button>
      <button>Home</button>
      //Here goes the right level
      <BottomBar />
    </div>
  );
};

export default GamePage;
